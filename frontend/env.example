# フロントエンド環境変数のテンプレート
#
# 【重要】VITE_ プレフィックスの変数はビルド成果物に埋め込まれ、
# ブラウザの開発者ツールから誰でも読み取れます。
# APIキー・シークレットの類は絶対にここへ書かないでください。
# OpenAI APIキーはバックエンドの OPENAI_API_KEY で管理します。

# バックエンドのオリジン（末尾に /api は付けない）
VITE_API_URL=http://localhost:8000

# 環境識別
VITE_ENVIRONMENT=development
